#include<stdio.h>
int main(){
	char str[50];
	int i,count=0;
	for(i=0;str[i]!='\0';i++){
		scanf("%c",&str[i]);
		if(str[i]==' '){
			count++;
		}
	}
	printf("Number of words = %d",count+1);
	return 0;
}
